# Convex Configuration
# Copy this file to .env.local and fill in the values

# Convex Deployment URL (from npx convex dev output)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# For production deployment
CONVEX_DEPLOY_KEY=your-deploy-key

# ============================================
# Google OAuth (Configure in Google Cloud Console)
# ============================================
# Set these in Convex dashboard using:
# npx convex env set AUTH_GOOGLE_ID <your_client_id>
# npx convex env set AUTH_GOOGLE_SECRET <your_client_secret>

# ============================================
# MIGRATION: Supabase (Keep for data export)
# ============================================
# Keep these until migration is complete
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# After migration is verified, remove the Supabase variables
